# unknown (used as the default when nothing is known about the text)
# this will cause an empty map to be used which will force all the text
# in the document to the default style

addsyntaxmap "Unknown" \
{
}
